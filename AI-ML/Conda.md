Install python 
Install Conda 

Open Conda prompt
Create Virtual Enviorment 
`conda create --name v_name`
`conda activate v_name`

v_name is usually referred as project name.


Now install the required libraries from notebook itself

e.g.
`!pip install numpy`


