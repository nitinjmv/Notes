version: "3.8"
services:
  app:
    build: ./app
    ports:
      - 5000:5000
    links:
      - db

  db:
    image: mysql:8.0
    ports:
      - 32000:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db:/docker-entrypoint-initdb.d/:ro
